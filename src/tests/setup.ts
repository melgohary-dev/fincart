/**
 * Test environment setup.
 *
 * Imports jest-dom matchers (`toBeInTheDocument`, `toHaveTextContent`,
 * etc.) so they are available globally in every test file without an
 * explicit import.
 */
import '@testing-library/jest-dom';
